import Jcrool from '../JcroolClass';

const data = {
    hello: "hi there sir",
    tood: 42
}
const j = new Jcrool(data);

describe("it finds pairs", () => {
    test("twin", () => {
        
    })
})